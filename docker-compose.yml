services:
  db:
    image: pgvector/pgvector:pg16
    container_name: docquery-db
    environment:
      POSTGRES_DB: docquery
      POSTGRES_USER: docquery
      POSTGRES_PASSWORD: localdev
    ports:
      - "5433:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: